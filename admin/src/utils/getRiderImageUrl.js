// Kept as a named re-export so existing rider imports don't need to change —
// the logic itself is generic and lives in getImageUrl.js.
export { getImageUrl as getRiderImageUrl } from "@/utils/getImageUrl";
