// Vitest setup file, referenced by the `test.setupFiles` entry in
// vite.config.js. It has no global setup to do yet  the suite is plain unit
// tests over pure helpers  but the file has to exist or vitest fails to load
// any test at all.
